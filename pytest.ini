[pytest]
testpaths = tests
addopts = -p no:cacheprovider
markers =
    live_browser: opt-in live browser provider integration checks
    live_obscura: opt-in live Obscura CLI integration checks
    live_obscura_cdp: opt-in live Obscura CDP integration checks
    live_playwright: opt-in live Playwright semantic observation checks
